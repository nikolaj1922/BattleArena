# ⚔ Auto Battle Arena — Test Task

## 📌 О проекте

Небольшой прототип 1v1 auto-battle системы, реализованный в Unity.  
Проект демонстрирует архитектурный подход к построению боевой системы с разделением логики персонажей, оружия, состояний, эффектов и UI.

**Основная цель** — показать умение проектировать расширяемую боевую архитектуру.

---

## 🏗 Архитектура

Проект построен на принципах:

- **Separation of Concerns**  
- **Composition over Inheritance**  
- **Data-driven подход** (через ScriptableObject)  
- **Расширяемость через интерфейсы**

---

## 🔹 Основные системы

### 1️⃣ Character

Композиционный объект, содержащий менеджеры:

- `CharacterStateManager`  
- `CharacterAttackManager`  
- `CharacterLocomotionManager`  
- `CharacterEffectManager`  
- `CharacterAnimationManager`  

Персонаж:

- хранит текущее здоровье  
- обрабатывает получение урона  
- управляет сменой состояний  
- уведомляет UI через событие `OnHealthChanged`  

---

### 2️⃣ State Machine

Реализована state-based архитектура:

- `IdleState`  
- `MoveState`  
- `AttackState`  
- `DeathState`  

Каждое состояние имеет:

- `Enter`  
- `Tick`  
- `FixedTick`  
- `Exit`  

---

### 3️⃣ Weapon System

Атака инкапсулирована в оружии: 

public abstract class Weapon : MonoBehaviour 

Оружие: 

- запускает анимацию
- рассчитывает урон     
- создает проджектайлы (если требуется)
- применяет статус-эффекты
- позволяет легко менять поведение персонажа заменой оружия

---
  
### 4️⃣ Status Effects System

Реализована система эффектов:

- `Stun`  
- `Poison`  
- `Damage modifiers`  

**Особенности:**

- эффекты создаются как runtime-инстансы  
- поддерживается длительность  
- реализована защита от дублирования  
- возможны модификаторы урона через `IDamageModifier`  

---

### 5️⃣ Damage Calculation

Финальный урон рассчитывается через список модификаторов:

- `IDamageModifier`  

Позволяет гибко добавлять:

- уменьшение урона  
- усиление  
- временные бафы/дебафы  

---

### 6️⃣ UI

- `HealthBar` (World Space Canvas)  
- Подписка через событие `OnHealthChanged`  
- Floating damage text с анимацией и fade-out  
- Билбординг (поворот к камере)  

---

### 7️⃣ BattleManager

Отвечает за:

- спавн персонажей  
- выдачу оружия  
- установку целей  
- запуск боя  

---

## 🧠 Ключевые решения

- Оружие управляет типом атаки (melee / projectile)  
- Эффекты передаются через `WeaponData` (data-driven)  
- Нет жесткой привязки персонажа к конкретному типу атаки  
- Используются события вместо прямых UI вызовов  
- Минимизирована логика в `MonoBehaviour`  

---

## 🚀 Возможности расширения

Архитектура позволяет легко добавить:

- новые типы оружия  
- новые статус-эффекты  
- баффы/дебаффы  
- новые состояния  
- AI поведение  
- мульти-юнит бои  

---

## 🎯 Итог

В рамках тестового задания реализована расширяемая, модульная боевая система с разделением ответственности и возможностью дальнейшего масштабирования.
